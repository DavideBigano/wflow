import { Box, Text } from 'ink';

/** Bespoke component: placeholder landing tab, not reused elsewhere, no state or behavior yet. */
export function HomeTopic() {
    return (
        <Box flexDirection="column">
            <Text dimColor>TBD — nothing here yet.</Text>
        </Box>
    );
}
